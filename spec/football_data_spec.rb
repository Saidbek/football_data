require "spec_helper"

describe FootballData do
  it "has a version number" do
    expect(FootballData::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
